[build]
  command = "npm run build"
  publish = ".next"  # ← CRITICAL: Must be ".next" not "."

[build.environment]
  NODE_VERSION = "20"

# Cache static assets
[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
    
